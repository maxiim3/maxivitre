dans le step 1, le zonage n'est pas a jour. se referer aux rules `.rules/` aussi
consommer les constantes depuis `business-rules.comnfig.ts` pour les regles metiers, chiffres, tarifs etc... qui ne sont pas correct dans les differentes sections (type client, type service, zone geographiqur)
